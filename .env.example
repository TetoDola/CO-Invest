# Base Mini App Configuration
# Copy this file to .env.local and update the values

NEXT_PUBLIC_ROOT_URL=http://localhost:3000

# ====================================================================
# REQUIRED: Your Vault Contract Address
# ====================================================================
# Deploy an ERC4626 vault contract to Base and add its address here
# This is where user USDC deposits go
NEXT_PUBLIC_VAULT_FACTORY_ADDRESS=0x0000000000000000000000000000000000000000

# ====================================================================
# REQUIRED: Base Chain RPC
# ====================================================================
# Option 1: Use free public RPC (works but may be slow/rate limited)
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org

# Option 2: Use Alchemy (faster, more reliable)
# NEXT_PUBLIC_BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Option 3: Use CDP Node (recommended, works best with OnchainKit)
# Leave blank if using CDP API key below
# NEXT_PUBLIC_BASE_RPC_URL=

# ====================================================================
# OPTIONAL: CDP API Keys (Recommended for Production)
# ====================================================================
# Get from https://portal.cdp.coinbase.com/ (free tier available)
# Benefits: Faster balance queries, higher rate limits, better reliability
# 
# If you don't have these, the app will use direct RPC calls instead
# (slower but functional)
#
# NEXT_PUBLIC_CDP_API_KEY=your_cdp_node_api_key
# CDP_DATA_API_KEY=your_cdp_data_api_key

# ====================================================================
# REQUIRED: Vercel Redis Storage
# ====================================================================
# Get from Vercel Dashboard > Storage > Create Redis Database
# Automatically set in Vercel deployment - no manual configuration needed!
# For local development, copy this value after creating your Redis database:
#
# REDIS_REDIS_URL=redis://default:your_password@your-redis-instance.upstash.io:6379
#
# Note: In production on Vercel, REDIS_REDIS_URL is injected automatically

# ====================================================================
# OPTIONAL: Other Settings
# ====================================================================
NEXT_PUBLIC_CHAIN_ID=8453

# USDC on Base (native, don't change this)
NEXT_PUBLIC_USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# Webhook Secret (change in production)
WEBHOOK_SECRET=development_secret_change_in_production
